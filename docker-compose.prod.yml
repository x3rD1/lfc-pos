services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: pos_backend_prod
    restart: always
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL: ${DATABASE_URL}
    command: sh -c "npx prisma migrate deploy && npm run start"
